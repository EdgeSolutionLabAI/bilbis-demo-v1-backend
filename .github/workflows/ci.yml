# Auto-generated by Bilbis
name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      # actions/setup-node uses Node.js from the runner; keep it current
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'pnpm'

      # Avoid relying on corepack-installed pnpm binaries in constrained CI
      - run: npm i -g pnpm@10.30.3
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
